import React, { useState } from "react";

export default function Home()  {
  const [verbe, setVerbe] = useState({ verbe: [], verbeLoaded: false});

  ataisLoaded: false
        };
  return <div>home</div>;
}
